<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match *.*>
  @type file
  path /fluentd/logs/${tag}.%Y-%m-%d.%H%M

  <buffer tag,time>
    @type file
    path /fluentd/logs/buffer
    timekey 5m
    timekey_wait 1m
  </buffer>
</match>