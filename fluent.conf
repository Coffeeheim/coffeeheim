<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match *.*>
  @type csv
  path /fluentd/logs/${tag}.%Y-%m-%d.%H%M

  <buffer tag,time>
    @type csv
    path /fluentd/logs/buffer
    timekey 1d
    timekey_wait 10m
  </buffer>
</match>